apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: kubernetes-production
  region: ap-south-1
  version: "1.36"

iam:
  withOIDC: true

managedNodeGroups:
  - name: production-nodes
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
