apiVersion: v1
kind: Service

metadata:
  name: {{ include "flask-postgres.fullname" . }}-flask-service

spec:
  type: {{ .Values.service.type }}

  selector:
    app: flask

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5006
