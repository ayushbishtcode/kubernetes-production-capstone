apiVersion: v1
kind: Service

metadata:
  name: {{ include "flask-postgres.fullname" . }}-postgres-service

spec:
  clusterIP: None

  selector:
    app: postgres

  ports:
    - port: 5432
      targetPort: 5432
