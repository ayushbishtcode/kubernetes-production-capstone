apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "flask-postgres.fullname" . }}-flask
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list"]
